/**
 * Interface for all game piece attack behaviors
 */
public interface AttackBehavior {
    /**
     * attack method that all behaviors need to have. Does not return anything
     */
    void attack();
}
